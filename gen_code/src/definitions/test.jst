var _gen = require('./gen');
var $output = {
   name: 'test',
   depends: ['Worker'],
   elem: [],
   amount: 10,
   options:
   [
      {
         name: 'id',
         type: 'varchar(10)',
         func: _gen.gen_fkey($tables, 'Worker', 'id')
      }
   ]
}
